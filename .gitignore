# Created by .ignore support plugin (hsz.mobi)
### JetBrains template
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and WebStorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff
.idea/
.idea/**/workspace.xml
.idea/**/tasks.xml
.idea/**/dictionaries
.idea/**/shelf

# Sensitive or high-churn files
.idea/**/dataSources/
.idea/**/dataSources.ids
.idea/**/dataSources.local.xml
.idea/**/sqlDataSources.xml
.idea/**/dynamic.xml
.idea/**/uiDesigner.xml

# Gradle
.idea/**/gradle.xml
.idea/**/libraries

# CMake
cmake-build-debug/
cmake-build-release/

# Mongo Explorer plugin
.idea/**/mongoSettings.xml

# File-based project format
*.iws

# IntelliJ
out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Cursive Clojure plugin
.idea/replstate.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

# Editor-based Rest Client
.idea/httpRequests
### Scala template
*.class
*.log
### macOS template
# General
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk
### SBT template
# Simple Build Tool
# http://www.scala-sbt.org/release/docs/Getting-Started/Directories.html#configuring-version-control

dist/*
target/
lib_managed/
src_managed/
project/boot/
project/plugins/project/
.history
.cache
.lib/


# Quantlib-specific: large crawled data and backtest outputs
/data/
/result/

# IDE / tooling dirs
/.bsp/
/.metals/
/.vscode/
/.specify/

# Claude Code — team-shared agents/commands are version-controlled;
# memory stays user-specific at ~/.claude/projects/<encoded>/memory/
/.claude/settings.local.json
/.claude/.last_session*
/.claude/memory/

# Python research — virtual env / local cache / ad-hoc outputs not version-controlled
/src/quantlib/cache.duckdb
/src/quantlib/.venv/
/src/quantlib/__pycache__/
/src/quantlib/out/
/src/quantlib/src/quantlib/
# Backtest output (regenerable from iter_*.py / validate_*.py)
/src/quantlib/strat_lab/results/
# Factor panel snapshot (regenerable from src/quantlib/strat_lab/raw_quarterly.py)
# raw_quarterly 已移 var/cache(由 /var/ 涵蓋)
# Experiment intermediate outputs (ad-hoc)
/src/quantlib/experiments/out/
*.pyc

# Feature-planning artifacts not part of main project source
/backend/
/frontend/
/specs/

# macOS / Bloop / misc
.DS_Store
/.bloop/
/.playwright-mcp/

# S 策略 live 執行期產物(每日計劃/成交 log,非源碼)
src/quantlib/trading/live/state/

# 日內 1 分 K 原始資料(可由 Shioaji 重抓,不進版控;程式碼在 src/quantlib/intraday/)
/src/quantlib/data/intraday/

# 券商憑證/私鑰:任何位置都不得進版控(2026-07-21 事件:憑證被放到 repo 根目錄,
# 當時只有 src/quantlib/.gitignore 有 *.pfx 規則,根目錄完全無防護 → 補全域規則)。
*.pfx
*.p12
*.pem
*.key
*.jks
*.keystore

# ── 版控邊界修補(2026-07-22 結構稽核)────────────────────────────────
# 病灶:`*.log` 對 `client.log.20260514` 這種**後綴帶日期**的檔名無效
# (git check-ignore 實測 NOT IGNORED)→ log/ 145 MB 一直躺在 untracked 清單裡,
# 使用者只好對 git status 視而不見,而視而不見正是 82 MB parquet 與每日重生的
# 5 MB HTML 悄悄留在版控裡的原因。規則要跟上目錄演進,否則邊界會持續失守。

# 每日重生的產物:留在版控等於每天塞一份全新 blob 進 git

# 本地資料快照與 runtime state(可重生,不進版控)

# src/quantlib/data/ 混住兩種東西,規則必須分開下:
#   ✗ intraday/ = 可重下載的原始 1 分 K(194 MB)→ 不進版控
#   ✓ revenue_first_seen / confcall_events / news_aliases = **不可重生的觀測紀錄**
#     (「我們哪一天第一次看到這筆營收揭露」無法從資料庫回推,它正是 S 事件驅動
#      進場的時間真相)+ 人工策展的別名表 → 必須進版控

# ── 三種生命週期,三條規則(2026-07-22 Phase 2 結構重整;唯一真源 src/quantlib/paths.py)
# var/ = 可重生產物(cache / 回測輸出 / 報告 / log / runtime state)。**整包忽略,
#        一行搞定** —— 舊做法靠人工維護一長串規則,規則永遠跟不上目錄演進,
#        於是 952 個檔卡在 git status 裡讓人視而不見(2026-07-22 事故根因)。
/var/
# data/ = 原始封存(爬回來的 CSV 與 1 分 K)。不可重生、但 51 GB 不進 git。
/data/
src/quantlib/market_data.db
.venv

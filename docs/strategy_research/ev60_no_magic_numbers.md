# 重新蒸餾方法論:全參數盤點與論證(2026-08-06)

**使用者指示**:「我不希望整個方法論裡面有任何魔術數字,所有的選擇、數字都需要經過論證。」

本文把方法論裡**每一個影響行為的數字與選擇**列出來,逐個判定為三類之一:

| 類別 | 定義 | 需要什麼 |
|---|---|---|
| **界限** | 物理、協定或資料的硬邊界,不含選擇 | 指名出處即可(交易所公告、資料起始日、小數位數) |
| **導出** | 由 repo 內可重跑的量測決定 | 指出量測程式與結果 |
| **魔術** | 說不出「這個值從哪個量測得來」 | **量出來,或刪掉讓不變式接手** |

判準來自專案鐵律:*說不出出處的數字就是魔術數字*。「沿用慣例」「看起來合理」「與舊工作可比」**都不是出處**。

---

## 一、已量出來的:兩個核心參數被推翻

量測程式:`src/quantlib/evergreen/ev60_threshold_study.py`(可重跑,結果落
`var/out/ev60_threshold_study.json`)。

### 方法:門檻的職責決定它的判準

直接問「多少 % 才算暴漲」沒有客觀答案——那是語義問題。可判定的是門檻的**職責**:
它是蒸餾的**訓練目標**,而訓練目標的好壞只有一個標準——

> 拿它當目標訓練出來的判別力,套到下游選股之後,賺不賺錢。

所以對每個候選值,用**只含站位當日可得資訊**的量化特徵訓練固定超參的 LightGBM
(train 2008-2015 / test 2016-2021,特徵、超參、切分全部固定,**唯一變動的是訓練目標**),
在 OOS 每個站位選前 15 檔(對齊下游標記 Agent 的實際預算上限),算那個池子的實際報酬。

**這個指標同時吃到稀有度、可學性、可捕捉率三件事,而且單位是錢。**

**前提與限制(寫在這裡而不是藏在結論裡)**:量化模型是質化判別力的**下界代理**——
量化學得到的任何判別者都學得到;量化學不到的,質化未必學不到。所以本研究定出的是
「目標定義的相對優劣」,不是質化系統的絕對能力。相對排序比絕對數值可信得多。

### 結果 1:標籤定義錯了(口徑分歧)

現行標籤是「未來 120 交易日**區間最高**還原報酬 ≥ θ」。但下游用移動停利出場——
**一檔漲到 +80% 再全部吐回去,標籤算正例,實際賺不到。** 標籤與付款的口徑不同。

稀有度全部對齊 6.38% 之後的實測:

| 標籤定義 | 對應門檻 | OOS AUC | 池 trail25 | 池 trail35 |
|---|---|---|---|---|
| `max_120d`(現行) | 80.0% | **0.6892** | 6.03% | 8.69% |
| `end_120d` | 56.9% | 0.6488 | 5.45% | 8.78% |
| **`trail25`(實際入帳)** | 51.2% | 0.6217 | **8.40%** | **10.36%** |

**現行標籤最好預測(AUC 最高),卻最不賺錢。** 優化錯的目標,預測得越準賠越多。

### 結果 2:門檻應該是 30%,不是 80%

以「實際入帳報酬」為標籤再掃門檻:

| θ | 基準率 | 正例數 | OOS AUC | 池 trail15 | 池 trail25 | 池 trail35 |
|---|---|---|---|---|---|---|
| 20% | 19.81% | 48,447 | 0.5531 | 7.66% | 10.11% | 10.79% |
| **30%** | **13.29%** | **32,506** | 0.5814 | **7.84%** | **10.73%** | **12.23%** |
| 40% | 9.27% | 22,675 | 0.6035 | 6.84% | 9.84% | 11.09% |
| 50% | 6.63% | 16,209 | 0.6203 | 6.29% | 9.12% | 11.17% |
| 80% | 2.81% | 6,872 | 0.6536 | 5.22% | 7.87% | 10.90% |
| 150% | 0.66% | 1,616 | 0.6575 | 3.27% | 6.51% | 8.92% |

**θ=30% 在三個不同的評估出場(15%/25%/35% 移動停利)下同時最高**——不是被自己的
評估指標寵出來的。對照現行設計(`max_120d` @ 80%)的池 trail25 = 6.21%,
**最佳組合多賺 73%**。

**注意 AUC 與錢的方向相反**:AUC 隨門檻單調上升到 ~125%(稀有的類別**更**有結構,
與「越稀有越像雜訊」的直覺相反),但錢在 30% 就見頂。原因是低門檻的池子命中率高得多
(top-15 命中 20.4% vs 5.6%),而移動停利本來就會把上檔削掉——極端大漲多出來的
上檔補不回命中率的損失。**只看 AUC 會選出一個賺不到錢的門檻。**

### 結果 3:配對鍵漏了最強的混淆因子

負例硬度帶掃描顯示,**每一格的最強單變量都是 `vol60`(60 日報酬波動)**,AUC 0.59~0.77。
而 EV57 的配對鍵是「站位 × 流動性十分位 × 動能十分位 × 產業」,**沒有波動率**。

對現行 432 檔樣本直接稽核殘留:

| 特徵 | AUC | 在配對鍵? |
|---|---|---|
| `vol60` | **0.5781** | ✗ |
| `ret60` | 0.5418 | ✗ |
| `ret20` | 0.5410 | ✗ |
| `ret120` | 0.5148 | ✗ |
| `pct_below_250d_high` | 0.4906 | ✗ |
| `adv_dec` | **0.4989** | ✓ |
| `mom_dec` | **0.4999** | ✓ |

配對鍵裡的兩個被控制到**近乎完美的 0.500**——證明配對機制本身有效;沒進配對鍵的
就漏出來。蒸餾器會把 `vol60` 學成「判別力」,而那是下游引擎本來就有的東西:
**花標記成本去學一件已經會的事,還讓報告上的質化貢獻看起來比真實的高。**

### 修法:傾向分數配對,取代手挑配對鍵

手挑配對鍵這件事本身就是魔術數字的來源——挑哪幾個維度、各切幾分位,全是選擇。
正確做法是**對量化模型的預測分數配對**(propensity matching):

    負例 = 同站位、量化模型預測分數最接近的非正例

如此**任何量化特徵都無法分開兩臂(依構造)**,蒸餾器測到的判別力就是「相對於整個
量化層的增量」——那才是質化系統真正該被評價的東西。而且它沒有任何要挑的維度或分位數。

---

## 二、全參數盤點

### EV57 抽樣

| # | 參數 | 值 | 判定 | 依據 / 待辦 |
|---|---|---|---|---|
| 1 | `SURGE_MIN` | 0.80 | **魔術 → 已導出** | 改 **0.30**,且標籤改為實際入帳報酬(§一) |
| 2 | 標籤定義 | 區間最高 | **魔術 → 已導出** | 改 **trail 實際入帳**(§一) |
| 3 | `NEAR_MISS` / `QUIET_MAX` | (.25,.50) / .15 | **魔術 → 刪除** | 由傾向分數配對取代;硬度改為**量出來的維度**而非手設帶 |
| 4 | 配對鍵 | 站位×流動×動能×產業 | **魔術 → 刪除** | 同上;殘留 `vol60` 0.578 是它漏掉的直接證據 |
| 5 | `PRIOR_DAYS` | 120 | **魔術 → 刪除** | 只為 `mom_dec` 配對而存在,配對改傾向分數後不再需要 |
| 6 | `FWD_DAYS` | 120 | **魔術 → 量測中** | horizon 60/120/250 掃描進行中 |
| 7 | `LIMIT_ERA_SPLIT` | 2015-06-01 | **界限** | 台股單日漲跌幅由 7% 放寬至 10% 的生效日 |
| 8 | `ERA_START` | 2008-01-01 | **界限** | tpex 資料自 2007-07 起,加 250 日暖身 |
| 9 | `FWD_MUST_END_BEFORE` | 2022-07-01 | **導出** | 下游標記期起點;前瞻窗越過它就與標記期重疊 |
| 10 | `ERA_END` | 2021-12-31 | **導出** | 由 #9 與 #6 反推 |
| 11 | `REGIME_WARMUP_DAYS` | 1200 | **導出** | 180 日暖身會把 2008 崩盤誤判為「修正」,實測後拉長 |
| 12 | regime `r12m` / `dd3y` 窗 | 250 / 750 | **魔術 → 待量** | 12 個月/3 年是慣例。待辦:掃窗長,看 regime 分類的**基準率分離度** |
| 13 | `--per-cell` | 18 | **魔術 → 待量** | 應由檢力導出:要偵測多大的效果、多少樣本才夠 |
| 14 | 分位數 | 10 | **魔術 → 隨 #4 刪除** | 傾向分數配對不需要分位 |
| 15 | tier 切點 | adv_dec ≥7 / ≥3 | **魔術 → 待量** | 分層用;待辦:改用等頻三分位(無選擇)或由基準率差異導出 |
| 16 | `--seed` | 20260727 | **無害** | 只影響可重現性,不影響期望值 |

### EV58 卡片與批次

| # | 參數 | 值 | 判定 | 依據 / 待辦 |
|---|---|---|---|---|
| 17 | `BATCH` | 6 | **魔術 → 待量** | 待辦:由「正負比例要能浮動到無法反推」的最小批量導出 |
| 18 | `ERA_BANDS` | 6 段 × 2-3 年 | **半導出** | 桶界落在 #7 的世代斷點上(已修);但「2-3 年一桶」仍是選擇。待辦:由**語彙汰換速度**量出來 |
| 19 | `rolling_max(250)` | 250 | **半界限** | ≈ 一年交易日數(實測 2008-2021 年均 246);取整為 250 |
| 20 | `adv20` 窗 | 20 | **半界限** | ≈ 一個月交易日數 |
| 21 | size 五分位 | 5 | **魔術 → 待量** | 卡片上的規模級距;待辦:由基準率的分位間差異導出格數 |
| 22 | 期別保留組選法 | 每世代最近一個 | **導出** | 見 `evergreen_redistill_sampling.md`:最近者離標記期最近(最強移植證據)+ 保住崩跌樣本 |

### 提示詞

| # | 參數 | 值 | 判定 | 依據 / 待辦 |
|---|---|---|---|---|
| 23 | thin 預算 | ≤8 查詢 / ≤5 材料 | **半導出** | 實測舊管線標記深度 3.5 筆/檔;5 是「留一點餘裕」的取整。待辦:改用舊管線深度分布的 p90 |
| 24 | 九格 | 9 格 | **結構** | 是清單不是門檻;三層(不朽一手源/新聞檔案/時光機社群)各自有存在理由 |
| 25 | `retrievability_score` ≥2 定義 | ≥2 個獨立外部來源 | **魔術 → 待量** | 待辦:由「幾個獨立來源之後,`no_news_verdict` 的判定不再改變」量出來 |
| 26 | 試跑及格線 | 6 檔中 ≥4 檔 score ≥2 | **魔術 → 刪除** | 及格線本身沒有出處;改為**報告分布**由人裁決,不設閘 |
| 27 | 批內正負比例 | 2:4 / 3:3 / 4:2 | **導出** | 必須浮動,否則 agent 由結構反推配對關係(守衛已鎖) |
| 28 | 字數上限 | ≤120/200/400/1200 字 | **無害** | 版面控制,不影響判定 |

### 統計與稽核

| # | 參數 | 值 | 判定 | 依據 |
|---|---|---|---|---|
| 29 | Wilson `z` | 1.96 | **界限** | 95% 雙尾常態分位,統計慣例 |
| 30 | `_Z_MAX` | 3.0 | **界限** | 3 個標準差,常規顯著性界線;非調校值 |
| 31 | `TOP_N` | 15 | **導出** | 標記提示詞明訂「上限 15 檔」 |
| 32 | `TRAIN_END` | 2015-12-31 | **半導出** | 約當期間中點。待辦:對齊 #7 的 2015-05-31 讓切分不跨世代 |
| 33 | `TRAILS` | .15/.25/.35 | **導出為敏感度帶** | 不取單一值;結論須在三者下一致才算數(已照做) |
| 34 | 指數自洽 `TOL` | 0.02 | **界限** | 指數報價取小數第二位,兩個報價的進位誤差合計 ≤0.01,留一倍餘裕 |
| 35 | 缺日 `WITNESSES` | 2 | **導出** | 單一證人可能自己就壞——實測指數在無交易的星期六長出 93-133 列幽靈 |
| 36 | ev59 併發/退避 | 2 / 1.0s / 6 次 | **導出** | 實測 4 併發 0.3s 基數仍殘留 100 次限流 |
| 37 | ev59 回看月數 | 12 | **繼承 #25** | 對齊提示詞 G1 的 12 個月窗;該窗本身待量 |

---

## 三、結論

- **已量出並推翻兩個核心參數**:標籤定義(區間最高 → 實際入帳)與門檻(80% → 30%),
  合計讓同技能下的池子報酬由 6.21% 提升到 10.73%。
- **已找出一個配對缺陷**:`vol60` 殘留 0.578,而它是每一組對比的最強單變量。
  修法是傾向分數配對,同時消滅「手挑配對鍵」這個魔術數字來源。
- **仍待量測 8 項**(#6 進行中、#12/13/15/17/18/21/25/32),每項都已指定量測方式。
- **界限與導出共 15 項**,各自寫得出出處。
- **刪除 4 項**(#3/#4/#5/#14):不是給它們找理由,是讓不變式(傾向分數配對)接手。

**待量測的項目在完成前,不得宣稱方法論無魔術數字。** 本文的價值在於它們現在是
一份具名清單,而不是散落在程式碼裡沒人記得的常數。
